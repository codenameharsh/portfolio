<!-- ========== Start Section ========== -->
<div class="progress-container">
      <div class="progress-bar" id="myBar"></div>
    </div>

    <nav class="navbar">
        <div class="nav-container">
            <div class="logo">
              <a href="https://codenameharsh.github.io/portfolio/#"><img src="logo.png" alt="" height="30" width="30"></a>
            </div>

            <div class="menu-toggle" id="mobile-menu">
              <span class="bar"></span>
              <span class="bar"></span>
              <span class="bar"></span>
          </div>
            

            <ul class="nav-links">
                <li><a href="https://codenameharsh.github.io/portfolio/#work">WORK</a></li>
                <li><a href="https://codenameharsh.github.io/portfolio/#about">ABOUT</a></li>
                <li><a href="https://codenameharsh.github.io/portfolio/#skills">SKILLS</a></li>
                <li><a href="https://codenameharsh.github.io/portfolio/#contact">CONTACT</a></li>
            </ul>
        </div>
    </nav>

    <section class="case-study-hero">
        <p class="subtitle-header">Case Study:</p>
        <h1 class="title">Yonderlust</h1>
        
        <div class="tag-container">
            <span class="tag">User Interface Design</span>
            <span class="tag">Mobile App</span>
            <span class="tag">48-hr Sprint</span>
        </div>
    </section>

    <section class="final-outcome">
        <div class="outcome-header">
        <p class="section-title-italic">impact & links</p>
            <div class="line-divider-long"></div>
            <h2 class="section-title-bold">FINAL OUTCOME</h2>
        </div>

        <div class="outcome-content">
            <p class="playfair summary-text">
            A <span class="outcome-italic">production-ready prototype</span> that proves you don’t need a framework to build a seamless user experience.
            </p>
        </div>

        <div class="cta-container">
            <a href="#" class="btn inter">LIVE PROJECT</a>
            <a href="#" class="btn inter">GITHUB REPO</a>
        </div>

        <div class="back-next">
            <a href="https://codenameharsh.github.io/portfolio/#" class="link"> <span class="back-next-pointer">↤</span> back</a>
            <!-- <a href="#" class="link"> <span class="back-next-pointer">↥</span></a> -->
            
            <a href="https://codenameharsh.github.io/portfolio/aura/aura_index.html" class="link">next <span class="back-next-pointer">↦ </span></a>
        </div>
    </section>



    <footer class="footer">
        <div class="footer-content">
            <p class="footer-copyright">&copy; 2026 Harshitha Rajendran</p>
            <p class="footer-tagline">Design mastered. Mischief managed.</p>
        </div>
    </footer>
<!-- ========== End Section ========== -->


css:

/* Custom Palette Variables */
:root {
--bg-cream: #FFF9F4;
--grey-text: #585858;
--red-accent: #D14D4D;
--bg-black: #000000;
--cream: #FFF9F4;
}

* {
margin: 0;
padding: 0;
box-sizing: border-box;
}

body {
background-color: var(--bg-cream);
color: var(--grey-text);
font-family: 'Inter', sans-serif;
line-height: 1.6;
}

.section {
margin-top: 5rem;
}

/* Progress bar */
/* Container: The background of the progress bar */
.progress-container {
width: 100%;
height: 4px; /* Thin and elegant */
background: transparent; /* Blends with your navbar/bg */
position: fixed;
top: 0; /* Or adjust to sit below your sticky nav */
z-index: 1001; /* Ensure it stays above the navbar */
}

/* The actual bar: Uses your red accent #D14D4D */
.progress-bar {
height: 4px;
background: #D14D4D;
width: 0%; /* Initial state */
transition: width 0.1s ease-out;
}

/* --- Navigation --- */
.logo{
cursor: pointer;
}

.logo:hover{
transform: translateY(2px);
transform: scale(1.25);
}
.navbar {
width: 100%;
padding: 1.5rem 10%; /* Slightly reduced padding for better scroll feel */
position: fixed;     /* Changed from absolute to fixed */
top: 0;
left: 0;
z-index: 1000;       /* Ensures it stays above all other content */
background-color: rgba(255, 249, 244, 0.95); /* Using #FFF9F4 with slight transparency */
backdrop-filter: blur(10px); /* Modern frosted glass effect */
transition: all 0.3s ease;   /* Smooth transition for any scroll effects */
/* border-bottom: 1px solid rgba(88, 88, 88, 0.1); */
box-shadow:rgba(0, 0, 0, 0.24) 0px 3px 8px;

}

.nav-container {
display: flex;
justify-content: space-between;
align-items: center;
max-width: 1400px;
margin: 0 auto;
}

.nav-links {
display: flex;
list-style: none;
gap: 3rem;
}

.nav-links a {
text-decoration: none;
color: var(--grey-text);
font-weight: 700;
font-size: 0.7rem;
letter-spacing: 1px;
}

.nav-links :hover{
color: #D14D4D;
transform: translateY(-2px);
font-size: 0.9rem;
transition: transform 0.2s ease;
}

/* --- Responsive Styles --- */

/* --- Hamburger Menu Icon --- */
.menu-toggle {
display: none; /* Hidden on desktop */
flex-direction: column;
cursor: pointer;
gap: 6px;
z-index: 1001; /* Stays above the overlay */
}

.bar {
width: 25px;
height: 2px;
background-color: var(--grey-text); /* #585858 */
transition: all 0.3s ease;
}

/* --- Mobile Menu Logic (Under 768px) --- */
@media (max-width: 768px) {
.menu-toggle {
display: flex; /* Show icon on mobile */
}


.nav-links {
    position: fixed;
    right: -100%; /* Hidden off-screen */
    top: 0;
    flex-direction: column;
    background-color: rgba(255, 249, 244, 0.98); /* #FFF9F4 */
    width: 100%;
    height: 100vh;
    justify-content: center;
    align-items: center;
    gap: 3rem;
    transition: 0.4s cubic-bezier(0.77, 0.2, 0.05, 1.0);
    z-index: 999;
}

.nav-links.active {
    right: 0; /* Slide in */
}

.nav-links a {
    font-size: 1.5rem; /* Large links for touch */
}

/* Animation: Hamburger to 'X' */
.menu-toggle.is-active .bar:nth-child(2) {
    opacity: 0;
}
.menu-toggle.is-active .bar:nth-child(1) {
    transform: translateY(8px) rotate(45deg);
    background-color: var(--red-accent); /* #D14D4D */
}
.menu-toggle.is-active .bar:nth-child(3) {
    transform: translateY(-8px) rotate(-45deg);
    background-color: var(--red-accent);
}



}

/* For Tablets (Screens smaller than 1024px) */
@media (max-width: 1024px) {
.navbar {
padding: 1.5rem 5%;
}


.nav-links {
    gap: 1.5rem; /* Tighten navigation spacing */
}



}

/* For Mobile Phones (Screens smaller than 480px) */
@media (max-width: 480px) {
.navbar .nav-links {
display: flex; /* Overriding your previous 'none' to allow the slide-in */
}


}

/* case study styles */
.case-study-hero {
  text-align: center;
  margin-top: 5rem;
  padding: 60px 20px;
  background-color: var(--bg-cream); /* Contrast against the #FFF9F4 tags */
  font-family:'Playfair Display', serif;
}

.subtitle-header {
  color: #585858;
  font-size: 2.2rem;
  font-weight: 700;
  margin-bottom: 5px;
    font-family: 'Playfair Display', serif;
}

.title {
  color: #D14D4D;
  font-size: 3.8rem;
  margin-top: 0;
  margin-bottom: 40px;
  font-weight: 700;
  line-height: 1.2;
font-family: 'Playfair Display', serif;
}

.tag-container {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 12px;
  max-width: 700px;
  margin: 0 auto;
  font-family:'Inter', sans-serif;
}

.tag {
  background-color: #FFF9F4;
  color: #585858;
  border: 1px solid #EBE4DE;
  padding: 10px 25px;
  border-radius: 50px;
  font-size: 0.9rem;
  font-weight: 700;
  display: inline-block;
  box-shadow: rgba(0, 0, 0, 0.15) 1.95px 1.95px 2.6px;
}

/* This forces the third tag to drop to a new line and center */
.full-width {
  flex-basis: 100%;
  width: max-content;
  margin: 0 auto;
}

/* section dividers styling */

.section-title-italic {
    font-family: 'Playfair Display', serif;
    font-style: italic;
    font-weight: bold;
    color: var(--red-accent);
    font-size: 1rem;
    padding-top: 1rem;
}

.section-title-bold {
    font-size: 1.5rem;
    font-family: 'Playfair Display', serif;
    letter-spacing: 2px;
    font-weight: 700;
}

.line-divider-long {
    height: .1rem;
    background-color: var(--grey-text);
    opacity: 50%;
    width: 300px;
    margin: 10px auto;
}



/* final outcome */
.final-outcome {
  max-width: 900px;
  margin: 100px auto;
  text-align: center;
  padding: 0 20px;
}

.outcome-header {
  margin-bottom: 60px;
}

.outcome-content{
  font-size: 3rem;
}

.outcome-italic{
  font-family: 'Playfair Display', serif;
    font-style: italic;
    /* font-weight: bold;; */
}

.outcome-sub{
    font-size: 1.5rem;

}

.outcome-red{
  font-family: 'Playfair Display', serif;
    /* font-style: italic; */
    font-weight: bold;
    color: var(--red-accent);
}


.summary-text {
  font-size: 3.5rem;
  line-height: 1.2;
  color: #585858;
  margin-bottom: 80px;
  word-spacing: 5px;
}

/* The elegant Drop Cap "A" */
.summary-text::first-letter {
  font-style: normal;
  font-size: 1.2em;
  margin-right: 2px;
}

.cta-container {
  display: flex;
  justify-content: center;
  gap: 30px;
  align-items: center;
}

.btn {
  background-color: #585858;
  color: var(--bg-cream);
  text-decoration: none;
  padding: 20px 45px;
  font-weight: 700;
  font-size: 1.1rem;
  letter-spacing: 1px;
  transition: all 0.3s ease;
  border: 2px solid transparent;
}

.btn:hover {
  background-color: var(--red-accent);
}
.back-next{
  padding-top: 2rem ;
  margin-top: 1rem;
  padding-bottom: 1rem;
  text-transform: uppercase;
  display: flex;
  justify-content:space-evenly;
  gap: 30px;
  align-items: center;
}


.link {
  background-color: var(--bg-cream);
    /* border: 3px solid #d14d4d1a; */
  /* border-radius: 20px; */
  /* border: 2px dashed var(--bg-cream); */
  color:var(--red-accent);
  text-decoration: none;
  padding: .5rem 2rem;
  font-weight: normal;
  font-size: 1rem;
  letter-spacing: 1px;
  transition: all 0.3s ease;
  /* box-shadow: rgba(0, 0, 0, 0.15) 1.95px 1.95px 2.6px; */
}

.link:hover {
  background-color: #d14d4d;
  color: var(--bg-cream);
}


/* Responsive Scaling */
@media (max-width: 768px) {
  .summary-text {
    font-size: 2.2rem;
  }
  
  .cta-container {
    flex-direction: column;
    gap: 15px;
  }
  
  .btn {
    width: 100%;
    max-width: 300px;
  }
}



/* --- Scroll Animation States --- */

/* Initial hidden state: invisible and shifted down slightly */
.reveal-hidden {
    opacity: 0;
    transform: translateY(30px);
    transition: all 0.8s cubic-bezier(0.25, 1, 0.5, 1);
}

/* Active visible state */
.reveal-active {
    opacity: 1;
    transform: translateY(0);
}

/* --- Footer Styles --- */
.footer {
/* Uses your palette background color */
background-color: #FFF9F4;


/* Thin grey line at the top as seen in the image */
border-top: 1px solid #DDE0E2;

padding: 60px 20px;
text-align: center;
width: 100%;



}

.footer-content {
max-width: 1200px;
margin: 0 auto;
}

.footer-copyright {
font-family: 'Inter', sans-serif;
font-size: 0.95rem;
color: #585858; /* Your Grey Palette color */
margin-bottom: 8px;
letter-spacing: 0.5px;
}

.footer-tagline {
font-family: 'Inter', sans-serif;
font-size: 1rem;
font-weight: 500;
/* Your Red Palette color for the tagline */
color: #D14D4D;
margin: 0;
}

/* Optional: Mobile adjustment */
@media (max-width: 768px) {
.footer {
padding: 40px 15px;
}
.footer-copyright, .footer-tagline {
font-size: 0.85rem;
}
}






javaScript:

//progress bar

document.addEventListener("DOMContentLoaded", () => {
    window.onscroll = function() { 
        updateProgressBar();
    };

    function updateProgressBar() {
        const winScroll = document.body.scrollTop || document.documentElement.scrollTop;
        const height = document.documentElement.scrollHeight - document.documentElement.clientHeight;
        
        // Avoid division by zero if the page isn't scrollable
        if (height > 0) {
            const scrolled = (winScroll / height) * 100;
            const bar = document.getElementById("myBar");
            if (bar) {
                bar.style.width = scrolled + "%";
            }
        }
    }
});

//shrink nav on scroll
window.onscroll = function() {
  const nav = document.querySelector('.navbar');
  if (window.pageYOffset > 50) {
    nav.style.padding = "1rem 10%";
    nav.style.boxShadow = "0 4px 20px rgba(0,0,0,0.05)";
  } else {
    nav.style.padding = "2rem 10%";
    nav.style.boxShadow = "none";
  }
};

//hamburger menu toggle
const menu = document.querySelector('#mobile-menu');
const menuLinks = document.querySelector('.nav-links');

menu.addEventListener('click', () => {
    // Toggles the 'X' animation on the hamburger
    menu.classList.toggle('is-active');
    // Toggles the slide-in menu
    menuLinks.classList.toggle('active');
    
    // Optional: Prevent body scroll when menu is open
    document.body.style.overflow = menuLinks.classList.contains('active') ? 'hidden' : 'auto';
});

// Close menu when a link is clicked
document.querySelectorAll('.nav-links a').forEach(link => {
    link.addEventListener('click', () => {
        menu.classList.remove('is-active');
        menuLinks.classList.remove('active');
        document.body.style.overflow = 'auto';
    });
});


// Intersection Observer for Scroll Animations
const observerOptions = {
    root: null, // Use the viewport
    threshold: 0.1, // Trigger when 10% of the element is visible
    rootMargin: "0px 0px -50px 0px" // Slightly offset for a better feel
};

const observer = new IntersectionObserver((entries, observer) => {
    entries.forEach(entry => {
        if (entry.isIntersecting) {
            entry.target.classList.add('reveal-active');
            // Stop observing once it has revealed
            observer.unobserve(entry.target);
        }
    });
}, observerOptions);

// Target all major sections and grid items
document.querySelectorAll('section, .mood-item, .skill-card, .translation-box').forEach(el => {
    el.classList.add('reveal-hidden'); // Add initial hidden state
    observer.observe(el);
});